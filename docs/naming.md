# Naming

Use short, explicit names that reflect intent and domain language.

Prefer `Gate`, `ScrollHint`, and `gateCopy` over generic names.

Keep folder structure stable:
- `components/blocks` for section-level components
- `components/ui` for reusable UI atoms
- `content` for centralized static copy
- `lib` for future utilities and motion logic
